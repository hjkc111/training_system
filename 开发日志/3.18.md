今天上午把昨天残留的,每个项目训练日独立开来之后新出现的训练日分析项目报错问题解决了,
然后给网络布线训练日修改成了跟光电项目训练一样的页面和接口,也就是支持不同的项目不同的比对方式了
然后也修改了config中新添加了PRESET网络布线的内容,写了个适配的版本
然后把首页也改了一下,可能稍微好看点?
剩下的问题
1，每个训练日中的五个项目结束后生成的总结没有显示出来
2，不知道为什么设置了每个项目不同的项目，但是创建的时候还是会创建出来全都是默认视频分析
下午，再次修改了首页，删去了跳转到源代码仓库的功能，并且将四个核心功能模块的链接连上了相应的页面，可以直接跳转
